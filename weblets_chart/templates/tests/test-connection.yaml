apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "weblets_chart.fullname" . }}-test-connection"
  labels:
    {{- include "weblets_chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "weblets_chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
